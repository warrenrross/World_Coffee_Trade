# ── Private / sensitive ──────────────────────────────────────────────────────
# local-only notes: credentials, deploy details, internal URLs
private.md
secure.md

# ── QA / local screenshots ───────────────────────────────────────────────────
# Playwright visual-QA screenshots; generated locally, not for version control
qa*.png
# Design annotation and reference images used during development
mobile_view.PNG
move_collapse_button.png
move_key.jpeg
move_key.heic
this_key.JPG

# ── Design annotations / editorial PDFs ──────────────────────────────────────
*.pdf

# ── macOS system files ────────────────────────────────────────────────────────
.DS_Store
__MACOSX/

# ── Python / Jupyter (if EDA notebooks are added here later) ─────────────────
__pycache__/
*.pyc
.ipynb_checkpoints/
*_profile_report.html
*_sweetviz_report.html

# ── Node (if a dev server or tooling is added) ────────────────────────────────
node_modules/
